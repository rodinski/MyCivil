$TL = $R tan ($delta_angle / 2.0);
$arc_L = $R * $delta_angle;
$E = $R * ( 1 / cos ($delta_angle / 2.0 ) -1 ) ;
$M = $R*( 1 - cos ( $delta_angle / 2.0 ) ;
$chord  = 2 * $R * sin ($delta_angle / 2.0) ;
